name: Push pipeline

on:
  push:
    branches:
      - "master"

permissions:
  contents: read

jobs:
  on_build:
    uses: ./.github/workflows/buildlib.yml@master
    with:
      build_docs: true
      configuration: debug
      library_name: LeoEcsSaber